on:
  push:
    branches:
      - beta

jobs:
  cd:
    uses: ./.github/workflows/release.yml
    with:
        manifestFile: ./keruta/beta/keruta-executor.yaml
        harborProject: "library"
        repository: "kigawa-net/kigawa-net-k8s"

    secrets:
      harborPass: ${{ secrets.HARBOR_PASS }}
      gitToken: ${{ secrets.GIT_TOKEN }}
